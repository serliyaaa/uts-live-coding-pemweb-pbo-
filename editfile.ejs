<!DOCTYPE html> 
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit File</title>
    <link rel="stylesheet" href="/styles5.css">
</head>
<body>
    <div class="container">
        <h1>Edit File</h1>
        <form action="/files/update/<%= file.id %>" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="title">Judul File:</label>
                <input type="text" id="title" name="title" value="<%= file.title %>" required>
            </div>
            <div class="form-group">
                <label for="description">Deskripsi:</label>
                <textarea id="description" name="description" rows="4" required><%= file.description %></textarea>
            </div>
            <div class="form-group">
                <label for="file">Pilih File Baru (Opsional):</label>
                <input type="file" id="file" name="file">
                <small>Biarkan kosong jika tidak ingin mengubah file.</small>
            </div>
            <div class="form-group">
                <button type="submit">Perbarui File</button>
            </div>
        </form>
    </div>
</body>
</html>
